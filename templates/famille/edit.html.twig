{% extends 'base.html.twig' %}

{% block title %}تعديل بيانات العائلة{% endblock %}

{% block body %}
<div class="page-header justify-content-between">
    <div class="page-title">
        <h2>تعديل بيانات العائلة</h2>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 mt-2">
                <li class="breadcrumb-item"><a href="{{ path('app_dashboard') }}">لوحة القيادة</a></li>
                <li class="breadcrumb-item"><a href="{{ path('app_famille_index') }}">العائلات</a></li>
                <li class="breadcrumb-item active">تعديل</li>
            </ol>
        </nav>
    </div>
    <div class="d-flex gap-2">
        <a href="{{ path('app_famille_index') }}" class="btn btn-light border">
            <i class="bi bi-arrow-right ms-1"></i> العودة
        </a>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-xl-9">
        <div class="card border-0 shadow-sm overflow-hidden" style="border-radius: var(--radius-bento);">
            <div class="card-body p-5">
                {{ include('famille/_form.html.twig', {'button_label': 'تحديث البيانات'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
