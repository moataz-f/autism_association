{% extends 'base.html.twig' %}

{% block title %}إضافة منتفع جديد{% endblock %}

{% block body %}
<div class="page-header d-flex justify-content-between align-items-center">
    <div>
        <h2 class="mb-0">إضافة طفل منتفع جديد</h2>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="{{ path('app_dashboard') }}">لوحة القيادة</a></li>
                <li class="breadcrumb-item"><a href="{{ path('app_beneficiaire_index') }}">المنتفعون</a></li>
                <li class="breadcrumb-item active">إضافة</li>
            </ol>
        </nav>
    </div>
    <a href="{{ path('app_beneficiaire_index') }}" class="btn btn-light">
        <i class="bi bi-arrow-right"></i> العودة للقائمة
    </a>
</div>

<div class="row justify-content-center">
    <div class="col-md-10">
        <div class="card shadow-sm border-0">
            <div class="card-body p-4">
                {{ include('beneficiaire/_form.html.twig', {'button_label': 'حفظ بيانات الطفل'}) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}