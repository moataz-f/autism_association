{% extends 'base.html.twig' %}

{% block title %}تعديل بيانات الصورة{% endblock %}

{% block body %}
<div class="page-header justify-content-between">
    <div class="page-title">
        <h2>تعديل بيانات الصورة في المعرض</h2>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb mb-0 mt-2">
                <li class="breadcrumb-item"><a href="{{ path('app_dashboard') }}">لوحة القيادة</a></li>
                <li class="breadcrumb-item"><a href="{{ path('admin_gallery_index') }}">معرض الصور</a></li>
                <li class="breadcrumb-item active">تعديل</li>
            </ol>
        </nav>
    </div>
    <a href="{{ path('admin_gallery_index') }}" class="btn btn-light border">
        <i class="bi bi-arrow-right ms-1"></i> العودة
    </a>
</div>

<div class="row justify-content-center">
    <div class="col-xl-9">
        <div class="card border-0 shadow-sm overflow-hidden" style="border-radius: var(--radius-bento);">
            <div class="card-body p-5">
                {{ include('admin/gallery/_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
